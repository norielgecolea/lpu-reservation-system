# Copy to .env and fill in real values. This file is committed (no secrets).
# IP/host of the backend API the frontend talks to.
BACKEND_IP=127.0.0.1
# Port the backend API listens on (omit for 80).
BACKEND_PORT=8080
# Port the SSR server listens on.
PORT=4000
